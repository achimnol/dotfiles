[http]
    sslverify = false
[alias]
    st = status
    pretty-log = log --branches --graph --tags --oneline --decorate
[user]
    name = {{username}}
    email = {{email}}
[color]
    ui = true
[core]
    autocrlf = false
[push]
    default = simple
%if _platform == "windows":
[credential]
    helper = wincred
%end
